package Model;

public enum  Enum{
    MORNING, AFTERNOON, NIGHT, OVERNIGHT //I added overnight to includes shifts in healthcare
} 

enum Role{
    STAFF, MANAGER, ADMIN //we can add more
}

enum EmployeeType{
    FULL_TIME, PART_TIME, CASUAL, CONTRACT
}

enum AssignmentStatus{
    PROPOSED, CONFIRMED, SWAPPED, DROPPED
}
